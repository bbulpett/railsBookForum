<%= form_for(topic) do |f| %>
  <% if topic.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(topic.errors.count, "error") %> prohibited this topic from being saved:</h2>

      <ul>
      <% topic.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>

  <div class="field">
    <%= f.label :last_poster_id %>
    <%= f.number_field :last_poster_id %>
  </div>

  <div class="field">
    <%= f.label :last_post_at %>
    <%= f.datetime_select :last_post_at %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
